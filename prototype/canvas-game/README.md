# Disco DJ Marco — prototype scaffold

Generated by MetaMax from an approved PrototypePlan (mode: **stubbed-framework**, stack: **canvas-game**). The engine (runtime.js/modules.js) is the in-repo canvas-game kit, copied verbatim; only the config is per-game.

**Run:** `serve_prototype.py` from this folder, or open `index.html` via a local server.

## Modules → epics

- `MENU` → rule-epic-flow
- `CHOOSE LOCATION` → rule-epic-flow
- `DJ GEAR` → rule-epic-flow
- `CORE GAME` → rule-epic-flow
- `RESULTS REWARDS` → rule-epic-mechanics
- `LOADING SCREEN` → rule-epic-flow

## Stubbed (declared, not built)

- MENU
- CHOOSE LOCATION
- DJ GEAR
- LOADING SCREEN

## Bespoke mechanics (the only generated code)

- RhythmPlay
